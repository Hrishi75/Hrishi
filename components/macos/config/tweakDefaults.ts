// Default tweak values + available wallpapers.
import type { TweakValues } from "../types";

export const TWEAK_DEFAULTS: TweakValues = { accent: "#007AFF", wallpaper: "wp-photo", dockGlass: true, clockWidget: true, spotifyWidget: true };

export const WALLS: Record<string, string> = { "wp-photo": "Photo", "wp-sonoma": "Sonoma", "wp-sequoia": "Sequoia", "wp-sunrise": "Sunrise", "wp-graphite": "Graphite" };
